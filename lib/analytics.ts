export const trackEvent = (..._args: unknown[]) => {};
export const incrementCheckCount = (): number => 0;
export const getCheckCount = (): number => 0;
export default { trackEvent, incrementCheckCount, getCheckCount };
